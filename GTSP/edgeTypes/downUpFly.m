% downUpFly
% this will make a v_Adj with UAV land, takeoff, fly on it
% INPUTS

% OUTPUTS

function [v_AdjNew, distances] = downUpFly(maxDistance, x, y, numPoints, numLevels, v_Cluster, v_ClusterLevel, groupedPoints)

end